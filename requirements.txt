# Полный набор зависимостей = базовый тир + голосовой.
#
# Проще всего не ставить это руками, а запустить установщик — он подберёт
# колесо torch под твою видеокарту и проверит внешние программы:
#
#   Windows:  .\install.ps1
#   Linux:    ./install.sh
#
# Если ставишь вручную, соблюдай порядок: сначала torch с нужным
# --index-url, иначе приедет сборка под CPU (подробности в requirements-voice.txt).

-r requirements-base.txt
-r requirements-voice.txt

# --- Опциональное ---
# pvporcupine  # wake-word режим porcupine|both
#
# ffmpeg — не пакет Python, ставится в систему. Нужен для голосовых в Telegram.
# Applio/RVC — отдельное приложение со своим окружением, ставится вручную.
# Оба описаны в README.
